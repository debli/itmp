概率论
～～～

1 抽样
------
  放回抽样，亦即每次抽样的元素可以是重复的，已经被抽出的元素下次仍然可以被抽取到
  
  a)  放回有序抽样：结果元素相同但是顺序不同仍然作为不同的基本事件，这实际是一个允许元素重复的排列问题，即从 M个元素中选取n个允许重复的排列总数。n个位置抽样的可能个数是M的n次方。
  b)  放回无序抽样：结果元素不完全重合才能视为不同的基本事件，这实际是个组合，即从M个元素中选取n个不允许重复的组合总数。
